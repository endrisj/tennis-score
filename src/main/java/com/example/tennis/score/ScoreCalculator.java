package com.example.tennis.score;

public class ScoreCalculator {

    public ScoreCalculator(String[] scoringSequence) {
        if (null != scoringSequence) {
            int length = scoringSequence.length;
        }
    }
}
